{
    "name": "kuria/enum",
    "description": "Emulated enumeration objects in PHP",
    "keywords": ["enum", "enumeration"],
    "license": "MIT",
    "require": {
        "php": ">=7.1"
    },
    "require-dev": {
        "kuria/dev-meta": "^0.5"
    },
    "autoload": {
        "psr-4": {
            "Kuria\\Enum\\": "src"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Kuria\\Enum\\": "tests"
        }
    }
}